<Window x:Class="BaroManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BaroPath"
        Height="780"
        Width="1580"
        MinHeight="680"
        MinWidth="1250"
        WindowStartupLocation="CenterScreen"
        Background="#0F1117"
        Foreground="#E9ECF1">

    <Window.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#0F1117"/>
        <SolidColorBrush x:Key="PanelBackground" Color="#171A22"/>
        <SolidColorBrush x:Key="PanelBackgroundAlt" Color="#1E222D"/>
        <SolidColorBrush x:Key="InputBackground" Color="#10131A"/>
        <SolidColorBrush x:Key="InputBorder" Color="#333849"/>
        <SolidColorBrush x:Key="InputBorderHover" Color="#4A5269"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#7C5CFF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#8F74FF"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#B84A5B"/>
        <SolidColorBrush x:Key="DangerHoverBrush" Color="#D85B70"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E9ECF1"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#9AA3B2"/>
        <SolidColorBrush x:Key="GridLineBrush" Color="#2A2F3D"/>
        <SolidColorBrush x:Key="SelectionBrush" Color="#2A3150"/>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <ContextMenu x:Key="ManagedItemContextMenu"
                     x:Shared="False"
                     DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="{DynamicResource Common.Open}"
                      Command="{Binding OpenItemCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <MenuItem Header="{DynamicResource Context.ShowInExplorer}"
                      Command="{Binding OpenInExplorerCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <MenuItem Header="{DynamicResource Common.Run}"
                      Command="{Binding RunItemCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <Separator/>

            <MenuItem Header="{DynamicResource Context.CopyPath}"
                      Command="{Binding CopyPathCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <MenuItem Header="{DynamicResource Common.Edit}"
                      Command="{Binding EditItemCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <Separator/>

            <MenuItem Header="{DynamicResource Context.FindMoved}"
                      Command="{Binding FindMovedSelectedItemCommand}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <MenuItem Header="{DynamicResource Context.UseEverythingPath}"
                      Command="{Binding UpdateSelectedItemPathFromEverythingCommand}"
                      IsEnabled="{Binding CanUseEverythingPath}"/>

            <Separator/>

            <MenuItem Header="{DynamicResource Context.RemoveCurrentList}"
                      Command="{Binding RemoveFromCurrentListCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>

            <MenuItem Header="{DynamicResource Context.DeleteEverywhere}"
                      Command="{Binding DeleteItemCommand}"
                      CommandParameter="{Binding SelectedItem}"
                      IsEnabled="{Binding HasSelectedItem}"/>
        </ContextMenu>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style TargetType="Border">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PanelBackgroundAlt}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#2A3040"/>
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="{StaticResource InputBorderHover}"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#343B50"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder"
                                        Property="Opacity"
                                        Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
        </Style>

        <Style x:Key="DangerButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
        </Style>

        <Style x:Key="ViewModeToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource PanelBackgroundAlt}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ModeBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ModeBorder" Property="BorderBrush" Value="{StaticResource InputBorderHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ModeBorder" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="ModeBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="IsHitTestVisible" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <Border x:Name="Box"
                                    Width="15"
                                    Height="15"
                                    CornerRadius="3"
                                    Background="{StaticResource InputBackground}"
                                    BorderBrush="{StaticResource InputBorder}"
                                    BorderThickness="1"
                                    Margin="0,0,6,0">
                                <Path x:Name="CheckMark"
                                      Data="M 3 7 L 6 10 L 12 3"
                                      Stroke="{StaticResource TextPrimary}"
                                      StrokeThickness="2"
                                      StrokeEndLineCap="Round"
                                      StrokeStartLineCap="Round"
                                      Visibility="Collapsed"/>
                            </Border>

                            <ContentPresenter VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box"
                                        Property="Background"
                                        Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Box"
                                        Property="BorderBrush"
                                        Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="CheckMark"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box"
                                        Property="BorderBrush"
                                        Value="{StaticResource InputBorderHover}"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Path x:Name="Arrow"
                                      Data="M 0 0 L 5 5 L 10 0 Z"
                                      Fill="{StaticResource TextSecondary}"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="{StaticResource InputBorderHover}"/>
                                <Setter TargetName="Arrow"
                                        Property="Fill"
                                        Value="{StaticResource TextPrimary}"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Style="{StaticResource ComboToggleButtonStyle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          ClickMode="Press"/>

                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="8,0,32,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>

                            <TextBox x:Name="PART_EditableTextBox"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"
                                     Background="{StaticResource InputBackground}"
                                     Foreground="{StaticResource TextPrimary}"
                                     BorderThickness="0"
                                     Margin="8,0,32,0"
                                     VerticalAlignment="Center"/>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Background="{StaticResource PanelBackground}"
                                            BorderBrush="{StaticResource InputBorderHover}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="4">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox"
                                        Property="Visibility"
                                        Value="Visible"/>
                                <Setter TargetName="ContentSite"
                                        Property="Visibility"
                                        Value="Hidden"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder"
                                        Property="Background"
                                        Value="#242A38"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder"
                                        Property="Background"
                                        Value="{StaticResource SelectionBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#242A38"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="{StaticResource PanelBackground}"/>
            <Setter Property="AlternatingRowBackground" Value="#141821"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#202532"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="Padding" Value="6,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#202636"/>
                </Trigger>

                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SlimThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="#7C5CFF"
                                CornerRadius="6"
                                Margin="2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SlimVerticalScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Background" Value="#121827"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Margin="2">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Opacity="0"
                                                  Focusable="False"/>
                                </Track.DecreaseRepeatButton>

                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SlimThumbStyle}"/>
                                </Track.Thumb>

                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Opacity="0"
                                                  Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
            
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="BaroPath"
                       FontSize="28"
                       FontWeight="Bold"
                       DockPanel.Dock="Left"/>

            <TextBlock Text="{DynamicResource App.Tagline}"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="13"
                       VerticalAlignment="Bottom"
                       Margin="12,0,0,4"/>
        </DockPanel>

<Expander Grid.Row="1"
          IsExpanded="{Binding IsItemFormExpanded}"
          Margin="0,0,0,12"
          Foreground="{StaticResource TextPrimary}"
          Background="{StaticResource PanelBackground}">

    <Expander.Header>
        <DockPanel>
            <TextBlock Text="{Binding FormTitleText}"
                       FontSize="17"
                       FontWeight="Bold"
                       DockPanel.Dock="Left"/>

            <TextBlock Text="{DynamicResource Form.Hint}"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="8,2,0,0"/>
        </DockPanel>
    </Expander.Header>

    <Border Background="{StaticResource PanelBackground}"
            Padding="12"
            Margin="0,8,0,0"
            BorderThickness="1"
            BorderBrush="{StaticResource InputBorder}"
            CornerRadius="10">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource Form.Name}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1"
                     Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6"/>

            <TextBlock Grid.Row="0" Grid.Column="2" Text="{DynamicResource Form.Type}" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="0" Grid.Column="3"
                      ItemsSource="{Binding ItemTypes}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Value"
                      SelectedValue="{Binding SelectedItemType}"
                      Margin="6"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource Form.Path}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding NewPath, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6"/>

            <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        Margin="6">
                <Button Content="{DynamicResource Form.File}"
                        Width="80"
                        Margin="0,0,6,0"
                        Command="{Binding ChooseFileCommand}"/>
                <Button Content="{DynamicResource Form.Folder}"
                        Width="80"
                        Command="{Binding ChooseFolderCommand}"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource Form.Arguments}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1"
                     Text="{Binding NewArguments, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6"/>

            <TextBlock Grid.Row="2" Grid.Column="2" Text="{DynamicResource Form.WorkingDirectory}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="3"
                     Text="{Binding NewWorkingDirectory, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="{DynamicResource Form.CustomIcon}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="1"
                     Text="{Binding NewIconPath, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="{DynamicResource Form.CustomIconHint}"
                     Margin="6"/>

            <StackPanel Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        Margin="6">
                <Button Content="{DynamicResource Common.Choose}"
                        Width="90"
                        Margin="0,0,6,0"
                        Command="{Binding ChooseIconCommand}"/>
                <Button Content="{DynamicResource Common.Auto}"
                        Width="70"
                        ToolTip="{DynamicResource Form.AutoIconHint}"
                        Command="{Binding ClearIconCommand}"/>
            </StackPanel>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource Form.Tags}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1"
                     Text="{Binding NewTags, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6"/>

            <StackPanel Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="6">
                <CheckBox Content="{DynamicResource Form.Favorite}"
                          IsChecked="{Binding NewIsFavorite}"
                          Margin="0,0,16,0"/>
                <CheckBox Content="{DynamicResource Form.RunWithManager}"
                          IsChecked="{Binding NewRunOnAppStart}"/>
            </StackPanel>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="{DynamicResource Form.Note}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="5" Grid.Column="1"
                     Text="{Binding NewNote, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6"/>

            <StackPanel Grid.Row="5"
                        Grid.Column="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="6">
                <Button Content="{Binding MainActionText}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="130"
                        Height="32"
                        Margin="0,0,6,0"
                        Command="{Binding AddItemCommand}"/>

                <Button Content="{DynamicResource Common.Cancel}"
                        Width="90"
                        Height="32"
                        Command="{Binding CancelEditCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Expander>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="270"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="330"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelBackground}"
                    Padding="10"
                    BorderThickness="1"
                    BorderBrush="{StaticResource InputBorder}"
                    CornerRadius="10">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{DynamicResource Lists.Title}"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1"
                               Text="{DynamicResource Lists.New}"
                               Foreground="{StaticResource TextSecondary}"
                               Margin="0,0,0,4"/>

                    <Grid Grid.Row="2" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                 Height="30"
                                 Text="{Binding NewCollectionName, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="{DynamicResource Lists.NewHint}"/>

                        <Button Grid.Column="1"
                                Content="{DynamicResource Common.Add}"
                                Margin="6,0,0,0"
                                Command="{Binding AddCollectionCommand}"/>
                    </Grid>

                    <Border Grid.Row="3"
                            Background="{StaticResource PanelBackgroundAlt}"
                            Padding="8"
                            Margin="0,0,0,12"
                            BorderThickness="1"
                            BorderBrush="{StaticResource InputBorder}"
                            CornerRadius="8">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="{DynamicResource Lists.Selected}"
                                       FontWeight="Bold"
                                       Margin="0,0,0,6"/>

                            <TextBox Grid.Row="1"
                                     Height="30"
                                     Text="{Binding SelectedCollectionName, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="{DynamicResource Lists.SelectedHint}"/>

                            <StackPanel Grid.Row="2"
                                        Orientation="Horizontal"
                                        Margin="0,8,0,0">
                                <Button Content="{DynamicResource Common.Rename}"
                                        Width="90"
                                        Margin="0,0,6,0"
                                        Command="{Binding RenameSelectedCollectionCommand}"/>

                                <Button Content="{DynamicResource Common.Delete}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Width="80"
                                        Command="{Binding DeleteSelectedCollectionCommand}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <DockPanel Grid.Row="4">
                        <TextBlock DockPanel.Dock="Bottom"
                                   Text="{DynamicResource Lists.DragHelp}"
                                   Foreground="{StaticResource TextSecondary}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,0"/>

                        <Button DockPanel.Dock="Top"
                                Content="{DynamicResource Lists.AllItems}"
                                Height="32"
                                Margin="0,0,0,8"
                                ToolTip="{DynamicResource Lists.AllItemsHint}"
                                AllowDrop="True"
                                DragOver="AllItemsButton_DragOver"
                                Drop="AllItemsButton_Drop"
                                Command="{Binding ShowAllCommand}"/>

                        <ListBox x:Name="CollectionsListBox"
                                 ItemsSource="{Binding Collections}"
                                 SelectedItem="{Binding SelectedCollection}"
                                 DisplayMemberPath="DisplayName"
                                 ToolTip="{DynamicResource Lists.DropHint}"
                                 AllowDrop="True"
                                 DragOver="CollectionListBox_DragOver"
                                 Drop="CollectionListBox_Drop"/>
                    </DockPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
        Background="{StaticResource PanelBackground}"
        Padding="10"
        Margin="0,0,0,10"
        BorderThickness="1"
        BorderBrush="{StaticResource InputBorder}"
        CornerRadius="10">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{DynamicResource Items.Search}"
                       Foreground="{StaticResource TextSecondary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Margin="0,0,8,0"/>

            <TextBox Grid.Column="1"
                     Height="34"
                     FontSize="15"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="{DynamicResource Items.SearchHint}"/>

            <Button Grid.Column="2"
                    Content="{DynamicResource Items.CheckPaths}"
                    Height="34"
                    Margin="8,0,0,0"
                    Command="{Binding CheckPathsCommand}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{DynamicResource Items.Filter}"
                       Foreground="{StaticResource TextSecondary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Margin="0,0,8,0"/>

            <ComboBox Grid.Column="1"
                      Height="34"
                       ItemsSource="{Binding FilterModes}"
                       DisplayMemberPath="DisplayName"
                       SelectedValuePath="Value"
                       SelectedValue="{Binding SelectedFilterMode}"/>

            <CheckBox Grid.Column="2"
                      Content="{DynamicResource Items.OnlyMissing}"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center"
                      IsChecked="{Binding ShowOnlyMissing}"/>

            <TextBlock Grid.Column="3"
                       Text="{Binding SelectedCollection.DisplayName, TargetNullValue='Все элементы', FallbackValue='Все элементы'}"
                       Foreground="{StaticResource TextSecondary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Margin="10,0,12,0"/>

            <ToggleButton Grid.Column="4"
                          Content="{DynamicResource Items.ListView}"
                          Height="34"
                          MinWidth="88"
                          Style="{StaticResource ViewModeToggleStyle}"
                          IsChecked="{Binding IsListView, Mode=OneWay}"
                          Command="{Binding ShowListViewCommand}"/>

            <ToggleButton Grid.Column="5"
                          Content="{DynamicResource Items.GridView}"
                          Height="34"
                          MinWidth="82"
                          Margin="6,0,0,0"
                          Style="{StaticResource ViewModeToggleStyle}"
                          IsChecked="{Binding IsGridView, Mode=OneWay}"
                          Command="{Binding ShowGridViewCommand}"/>
        </Grid>
    </Grid>
</Border>

                <TextBlock Grid.Row="1"
                           Margin="2,0,0,8"
                           Foreground="{StaticResource TextSecondary}"
                           Text="{DynamicResource Items.InteractionHint}"/>
                
                <Expander Grid.Row="2"
                          Header="{DynamicResource Everything.Header}"
          Foreground="{StaticResource TextPrimary}"
          Background="{StaticResource PanelBackground}"
          Margin="0,0,0,10">

    <Border Background="{StaticResource PanelBackground}"
            BorderBrush="{StaticResource InputBorder}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="10"
            Margin="0,8,0,0">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="170"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Height="32"
                         Text="{Binding EverythingQuery, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="{DynamicResource Everything.QueryHint}"/>

                <TextBox Grid.Column="1"
                         Height="32"
                         Margin="6,0,0,0"
                         Text="{Binding EverythingMaxResults, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="{DynamicResource Everything.MaxResultsHint}"/>

                <Button Grid.Column="2"
                        Content="{DynamicResource Everything.Search}"
                        Height="32"
                        Margin="6,0,0,0"
                        Command="{Binding SearchEverythingCommand}"/>

                <Button Grid.Column="3"
                        Content="{DynamicResource Common.Open}"
                        Height="32"
                        Margin="6,0,0,0"
                        Command="{Binding OpenEverythingResultCommand}"
                        IsEnabled="{Binding HasSelectedEverythingResult}"
                        CommandParameter="{Binding SelectedEverythingResult}"/>

                <Button Grid.Column="4"
                        Content="{DynamicResource Context.ShowInExplorer}"
                        Height="32"
                        Margin="6,0,0,0"
                        Command="{Binding OpenEverythingResultInExplorerCommand}"
                        IsEnabled="{Binding HasSelectedEverythingResult}"
                        CommandParameter="{Binding SelectedEverythingResult}"/>

                <Button Grid.Column="5"
                        Content="{DynamicResource Common.Copy}"
                        Height="32"
                        Margin="6,0,0,0"
                        Command="{Binding CopyEverythingResultPathCommand}"
                        IsEnabled="{Binding HasSelectedEverythingResult}"
                        CommandParameter="{Binding SelectedEverythingResult}"/>

                <Button Grid.Column="6"
                        Content="{DynamicResource Everything.Add}"
                        Height="32"
                        Margin="6,0,0,0"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding AddEverythingResultCommand}"
                        IsEnabled="{Binding HasSelectedEverythingResult}"
                        CommandParameter="{Binding SelectedEverythingResult}"/>
            </Grid>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding EverythingResults}"
                      SelectedItem="{Binding SelectedEverythingResult}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      RowHeight="28"
                      FontSize="12">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Common.Type}"
                                        Binding="{Binding TypeDisplay}"
                                        Width="110"/>

                    <DataGridTextColumn Header="{DynamicResource Grid.Name}"
                                        Binding="{Binding Name}"
                                        Width="220"/>

                    <DataGridTextColumn Header="{DynamicResource Common.Path}"
                                        Binding="{Binding Path}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</Expander>

                <Grid Grid.Row="3">
                    <DataGrid x:Name="MainItemsGrid"
                              ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem}"
                              Visibility="{Binding IsListView, Converter={StaticResource BooleanToVisibilityConverter}}"
                              ContextMenu="{StaticResource ManagedItemContextMenu}"
                              PreviewMouseLeftButtonDown="MainItemsGrid_PreviewMouseLeftButtonDown"
                              PreviewMouseRightButtonDown="MainItems_PreviewMouseRightButtonDown"
                              MouseMove="MainItemsGrid_MouseMove"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              MouseDoubleClick="MainItemsGrid_MouseDoubleClick"
                              KeyDown="MainItemsGrid_KeyDown"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              SelectionMode="Single"
                              RowHeight="32"
                              FontSize="13">

                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="★"
                                                    Binding="{Binding IsFavorite}"
                                                    Width="40"/>

                            <DataGridTextColumn Header="{DynamicResource Grid.Status}"
                                                Binding="{Binding StatusDisplay}"
                                                Width="150"/>

                            <DataGridTextColumn Header="{DynamicResource Common.Type}"
                                                Binding="{Binding TypeDisplay}"
                                                Width="120"/>

                            <DataGridTextColumn Header="{DynamicResource Common.Name}"
                                                Binding="{Binding Title}"
                                                Width="190"/>

                            <DataGridTextColumn Header="{DynamicResource Common.Path}"
                                                Binding="{Binding Path}"
                                                Width="*"/>

                            <DataGridTextColumn Header="{DynamicResource Grid.Tags}"
                                                Binding="{Binding Tags}"
                                                Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ListBox x:Name="MainItemsTiles"
                             ItemsSource="{Binding Items}"
                             SelectedItem="{Binding SelectedItem}"
                             Visibility="{Binding IsGridView, Converter={StaticResource BooleanToVisibilityConverter}}"
                             ContextMenu="{StaticResource ManagedItemContextMenu}"
                             PreviewMouseLeftButtonDown="MainItemsGrid_PreviewMouseLeftButtonDown"
                             PreviewMouseRightButtonDown="MainItems_PreviewMouseRightButtonDown"
                             MouseMove="MainItemsGrid_MouseMove"
                             MouseDoubleClick="MainItemsGrid_MouseDoubleClick"
                             KeyDown="MainItemsGrid_KeyDown"
                             SelectionMode="Single"
                             Padding="8"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">

                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem"
                                   BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                <Setter Property="Width" Value="132"/>
                                <Setter Property="Height" Value="126"/>
                                <Setter Property="Margin" Value="5"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8"
                                        ToolTip="{Binding Path}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="76"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <Image Grid.Row="0"
                                               Width="68"
                                               Height="68"
                                               Source="{Binding IconSource}"
                                               Stretch="Uniform"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>

                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding Title}"
                                                   TextAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   TextTrimming="CharacterEllipsis"
                                                   FontWeight="SemiBold"
                                                   LineHeight="16"
                                                   MaxHeight="34"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>

            <Border Grid.Column="4"
                    Background="{StaticResource PanelBackground}"
                    Padding="12"
                    BorderThickness="1"
                    BorderBrush="{StaticResource InputBorder}"
                    CornerRadius="10">

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <ScrollViewer.Resources>
                        <Style TargetType="{x:Type ScrollBar}"
                               BasedOn="{StaticResource SlimVerticalScrollBarStyle}"/>
                    </ScrollViewer.Resources>

                    <StackPanel>
<TextBlock Text="{DynamicResource Sidebar.SelectedItem}"
           FontSize="15"
           FontWeight="Bold"
           Margin="0,0,0,8"/>

<Expander Header="{DynamicResource Settings.Title}"
          IsExpanded="False"
          Foreground="{StaticResource TextPrimary}"
          Background="{StaticResource PanelBackground}"
          Margin="0,0,0,12">

    <Border Background="{StaticResource PanelBackgroundAlt}"
            BorderBrush="{StaticResource InputBorder}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="10"
            Margin="0,8,0,0">

        <StackPanel>
            <TextBlock Text="{DynamicResource Settings.Language}"
                       Foreground="{StaticResource TextSecondary}"
                       Margin="0,0,0,4"/>

            <ComboBox ItemsSource="{Binding LanguageOptions}"
                      SelectedItem="{Binding SelectedLanguage}"
                      Height="34"
                      Margin="0,0,0,12"/>

            <TextBlock Text="Everything / ES"
                       FontSize="15"
                       FontWeight="Bold"
                       Margin="0,0,0,8"/>

            <TextBlock Text="es.exe"
                       Foreground="{StaticResource TextSecondary}"
                       Margin="0,0,0,4"/>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SettingsEsPath, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="{DynamicResource Settings.EsPathHint}"/>

                <Button Grid.Column="1"
                        Content="{DynamicResource Common.Choose}"
                        Margin="6,0,0,0"
                        Command="{Binding ChooseEsPathCommand}"/>
            </Grid>

            <TextBlock Text="Everything.exe"
                       Foreground="{StaticResource TextSecondary}"
                       Margin="0,0,0,4"/>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SettingsEverythingPath, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="{DynamicResource Settings.EverythingPathHint}"/>

                <Button Grid.Column="1"
                        Content="{DynamicResource Common.Choose}"
                        Margin="6,0,0,0"
                        Command="{Binding ChooseEverythingPathCommand}"/>
            </Grid>

            <CheckBox Content="{DynamicResource Settings.AutoStartEverything}"
                      IsChecked="{Binding SettingsAutoStartEverything}"
                      Margin="0,0,0,12"/>

            <UniformGrid Columns="2" Margin="0,0,0,8">
                <Button Content="{DynamicResource Common.Save}"
                        Height="34"
                        Margin="0,0,6,6"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding SaveSettingsCommand}"/>

                <Button Content="{DynamicResource Common.Test}"
                        Height="34"
                        Margin="0,0,0,6"
                        Command="{Binding TestEverythingSettingsCommand}"/>

                <Button Content="{DynamicResource Settings.SettingsFolder}"
                        Height="34"
                        Margin="0,0,6,6"
                        Command="{Binding OpenSettingsFolderCommand}"/>

                <Button Content="{DynamicResource Settings.ToolsFolder}"
                        Height="34"
                        Margin="0,0,0,6"
                        Command="{Binding OpenToolsFolderCommand}"/>
            </UniformGrid>

            <TextBlock Text="{Binding SettingsFilePath}"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="11"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</Expander>

                        <Border Background="{StaticResource PanelBackgroundAlt}"
                                Padding="10"
                                BorderThickness="1"
                                BorderBrush="{StaticResource InputBorder}"
                                CornerRadius="8"
                                Margin="0,0,0,12">

                            <StackPanel>
                                <TextBlock Text="{Binding SelectedItem.Title, TargetNullValue='—'}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,8"/>

                                <TextBlock Text="{Binding SelectedItem.TypeDisplay, TargetNullValue='—'}"
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="0,0,0,4"/>

                                <TextBlock Text="{Binding SelectedItem.StatusDisplay, TargetNullValue='—'}"
                                           Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{DynamicResource Sidebar.Actions}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Margin="0,0,0,8"/>

                        <UniformGrid Columns="2" Margin="0,0,0,12">
                            <Button Content="{DynamicResource Common.Open}"
                                    Height="34"
                                    Margin="0,0,6,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding OpenItemCommand}"
                                    CommandParameter="{Binding SelectedItem}"/>

                            <Button Content="{DynamicResource Context.ShowInExplorer}"
                                    Height="34"
                                    Margin="0,0,0,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding OpenInExplorerCommand}"
                                    CommandParameter="{Binding SelectedItem}"/>

                            <Button Content="{DynamicResource Common.Run}"
                                    Height="34"
                                    Margin="0,0,6,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding RunItemCommand}"
                                    CommandParameter="{Binding SelectedItem}"/>

                            <Button Content="{DynamicResource Common.Copy}"
                                    Height="34"
                                    Margin="0,0,0,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding CopyPathCommand}"
                                    CommandParameter="{Binding SelectedItem}"/>

                            <Button Content="{DynamicResource Common.Edit}"
                                    Height="34"
                                    Margin="0,0,6,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding EditItemCommand}"
                                    CommandParameter="{Binding SelectedItem}"/>

                            <Button Content="{DynamicResource Sidebar.RemoveList}"
                                    Height="34"
                                    Margin="0,0,0,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding RemoveFromCurrentListCommand}"
                                    CommandParameter="{Binding SelectedItem}"/>

                            <Button Content="{DynamicResource Sidebar.FindMoved}"
                                    Height="34"
                                    Margin="0,0,6,6"
                                    IsEnabled="{Binding HasSelectedItem}"
                                    Command="{Binding FindMovedSelectedItemCommand}"/>

                            <Button Content="{DynamicResource Sidebar.UseEsPath}"
                                    Height="34"
                                    Margin="0,0,0,6"
                                    IsEnabled="{Binding CanUseEverythingPath}"
                                    Command="{Binding UpdateSelectedItemPathFromEverythingCommand}"/>
                        </UniformGrid>

                        <Button Content="{DynamicResource Context.DeleteEverywhere}"
                                Style="{StaticResource DangerButtonStyle}"
                                Height="36"
                                Margin="0,0,0,16"
                                IsEnabled="{Binding HasSelectedItem}"
                                Command="{Binding DeleteItemCommand}"
                                CommandParameter="{Binding SelectedItem}"/>

                        <TextBlock Text="{DynamicResource Common.Path}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Margin="0,0,0,6"/>

                        <TextBox Text="{Binding SelectedItem.Path, TargetNullValue=''}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 MinHeight="60"
                                 VerticalContentAlignment="Top"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="{DynamicResource Grid.Tags}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Margin="0,0,0,6"/>

                        <TextBox Text="{Binding SelectedItem.Tags, TargetNullValue=''}"
                                 IsReadOnly="True"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="{DynamicResource Sidebar.WorkingDirectory}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Margin="0,0,0,6"/>

                        <TextBox Text="{Binding SelectedItem.WorkingDirectory, TargetNullValue=''}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 MinHeight="46"
                                 VerticalContentAlignment="Top"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="{DynamicResource Sidebar.Note}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Margin="0,0,0,6"/>

                        <TextBox Text="{Binding SelectedItem.Note, TargetNullValue=''}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 MinHeight="90"
                                 VerticalContentAlignment="Top"/>
                        
                        <TextBlock Text="{DynamicResource Settings.Maintenance}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Margin="0,16,0,6"/>

                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <Button Content="{DynamicResource Settings.ExportJson}"
                                    Height="34"
                                    Margin="0,0,6,6"
                                    Command="{Binding ExportBackupCommand}"/>

                            <Button Content="{DynamicResource Settings.ImportJson}"
                                    Height="34"
                                    Margin="0,0,0,6"
                                    Command="{Binding ImportBackupCommand}"/>
                        </UniformGrid>

                        <Button Content="{DynamicResource Settings.DatabaseFolder}"
                                Height="34"
                                Command="{Binding OpenDatabaseFolderCommand}"/>
                        
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
